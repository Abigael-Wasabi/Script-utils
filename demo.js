function findMax(num1, num2, num3) {
    let max = num1;
    if (num2 > max) {
      max = num2;
    }
    if (num3 > max) {
      max = num3;
    }
    return max;
  }
  
console.log(findMax(4, 7, 2)); // Output: 7
  

let l=prompt("Enter a random number");
        let m=prompt("Enter a random number");
        let n=prompt("Enter a random number");
        let p=Math.max(l,m,n);
        alert(p + " " + "is the maximum number")


switch (age){
  case 1:
    17
    alert("You are 17")
    break;
}
